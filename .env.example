# Example environment variables — copy to .env.local and fill in your values

# URL of the Express backend (Neon DB)
# In production, set this to your deployed server URL e.g. https://api.earney.in/api
VITE_API_URL=http://localhost:3001/api
